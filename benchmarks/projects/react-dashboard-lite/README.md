# React Dashboard Lite

This pack simulates a small frontend module and doubles as a public demo fixture for Agentic Workforce.

Keep changes minimal and update docs when behavior changes.

Suggested public demo prompt:

`Add a compact status summary card to the dashboard and update tests or docs if needed.`
